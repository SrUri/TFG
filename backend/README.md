# Subject Comparison API

A FastAPI application for comparing university subjects by extracting and analyzing syllabi from URLs.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   brew install ollama
   ollama pull llama3

2. Run install_argos.py

3. Execute:
   ```bash
   uvicorn main:app --reload